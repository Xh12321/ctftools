# Misc 方向 Agent 规则

Misc 题先做宽而浅的格式识别，再根据证据选择路径。图像按元数据、像素通道、附加数据、二维码和隐写顺序检查；音视频检查容器、轨道、频谱、帧和字幕；文本检查编码、压缩、分隔和协议。

优先保存转换前后的文件和命令参数。使用 ImageMagick、FFmpeg、Steghide、ZBar 时限制输入大小和递归深度，保留原始附件，不把偶然出现的可读字符串直接视为 Flag。
