# Web 方向 Agent 规则

先确认题目授权的 URL、端口和范围，再做 HTTP 基线：响应码、Header、Cookie、重定向、技术栈和入口枚举。优先使用 `curl`、`httpx`、`whatweb` 和有限深度的 `gobuster`；只有在确认存在参数后才使用 `sqlmap`。

每次请求记录方法、URL、参数、响应摘要和是否修改服务端状态。爆破/枚举设置线程、字典、速率和超时上限；禁止扫描题目范围外的地址。涉及登录、文件上传、SSRF、命令执行或数据库写入时，先保留原始响应并在报告中标出影响。
