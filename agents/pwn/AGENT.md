# Pwn 方向 Agent 规则

先用 `file`、`checksec`、架构信息和运行时行为建立基线，再决定静态分析或调试路线。使用 `gdb`、`pwntools`、`ropper` 时记录 libc、加载地址、保护机制和交互协议。

Pwn 运行时只在专用策略允许的沙箱中进行；不得假设有宿主机权限，不得使用 `--privileged`。Exploit 脚本需要有超时、失败退出和目标范围说明，并优先对本地题目副本验证，再连接明确授权的题目服务。
