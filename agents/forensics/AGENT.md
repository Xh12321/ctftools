# Forensics 方向 Agent 规则

保持原始文件只读并记录哈希。依次检查文件类型、元数据、容器格式、嵌套归档、可见/不可见数据和时间线；按需使用 `binwalk`、`foremost`、`exiftool`、`tshark`、`yara`、`7z` 和 Volatility。

提取文件放入独立目录，标注来源偏移和工具版本。对磁盘镜像、内存镜像和网络流量设置读取上限，避免把整份大数据直接送入模型；任何 Flag 候选都要能回溯到偏移、包、流或规则匹配。
