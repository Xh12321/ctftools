# 通用取证工作流

1. `pwd`、`find`、`file`、哈希和大小盘点输入。
2. 将原始附件视为只读；分析输出放在 `analysis/`、`evidence/` 或 `scripts/`。
3. 对输出设置合理上限，避免把大文件、无限循环或压缩炸弹直接加载进上下文。
4. 记录命令及关键输出；脚本使用固定输入和明确退出码。
5. 结论引用证据路径，并在 `WRITEUP.md` 中写出复现顺序。
