# Forensics Skill

## 目标

让每个提取结果都能回溯到原始数据的偏移、包、流或规则。

## 建议顺序

1. 原始哈希、文件类型、大小、元数据和目录树。
2. 容器/归档/嵌套层次与可见数据检查。
3. 按证据选择 `binwalk`、`foremost`、`exiftool`、`7z`、`tshark`、`yara` 或 Volatility。
4. 提取到带序号的目录，记录偏移、过滤条件和工具版本。
5. 对时间线、网络流、内存结构或隐藏数据进行独立验证。

避免无限递归、大文件全量上下文和未限制的解码器；Flag 候选必须给出明确来源。
