# Misc Skill

## 目标

先识别数据形态，再用最小转换验证隐藏信息，保证原始证据可恢复。

## 建议顺序

1. 检查编码、文件签名、元数据、容器、附加数据和尺寸。
2. 图像：通道/位平面、EXIF、尾随数据、二维码和隐写。
3. 音视频：轨道、字幕、帧、频谱、采样和容器元数据。
4. 文本/协议：编码、压缩、分隔、校验和状态机。
5. 保存每次转换的输入哈希、参数和输出，最终用 Flag 格式或独立脚本验证。

使用 FFmpeg、ImageMagick、Steghide、ZBar 时限制输入和递归深度，不覆盖原始附件。
